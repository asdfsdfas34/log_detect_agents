ttls - test time loop scaling

loop eng.

agent > reason , tool , memory > tool + memory 가 되고 있음

react => agent loop

event driven - hill climbing loop > 로깅을 통한 자기개선

- 
Doc-to-LoRA

embedding 모델
e5 , BGE

검색은 query token , document tokens 의 관계

attention
 -> 각 단어가 다른 모든 단어를 참고해서 의미를 업데이트 하는 과정.


벡터간 대조훈련이 필요.

dense ret. > rerank

late interaction
- colbert
- colpali

tree retrival

--
문서의 중요한 chunk ! signal


agent core - llm(reasoning) , tool , memory

tool-memory - skill , protocol, outside of harness
> 나만의 코어 - 외부에 저장된 지식

loop eng.(skill ops - skill 의 loop eng.> 지식 library)
> hill climbing loop = to be model / 시계열 데이터가 추가 되어야 tool 의 최종 하네스가 될거다.
---------------------------

agent run time
- 

----------------------------
> memory
mem0 비정형
letta 메모리구조

langmem 

zep 정형 시간그래프
cognee 지식그래프

읽기의 자율성 , 쓰기의 규정 강제.
'napmem 계층식 그래프'